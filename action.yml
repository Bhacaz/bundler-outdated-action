name: Outdated gems
description: Automatically update a issue with outdated gems
inputs:
  GH_TOKEN:
    description: GitHub token secret
    required: true
  REPOSITORY:
    description: ':username/:repo'
    required: true
runs:
  using: 'node12'
  main: 'main.js'
branding:
  icon: bookmark
  color: purple
